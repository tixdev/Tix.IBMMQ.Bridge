* Queues for reliability tests
DEFINE QLOCAL('RELIABILITY.IN')
SET AUTHREC PROFILE('RELIABILITY.IN') OBJTYPE(QUEUE) PRINCIPAL('app') AUTHADD(ALLMQI)
DEFINE QLOCAL('RELIABILITY.OUT') MAXDEPTH(95)
SET AUTHREC PROFILE('RELIABILITY.OUT') OBJTYPE(QUEUE) PRINCIPAL('app') AUTHADD(ALLMQI)
