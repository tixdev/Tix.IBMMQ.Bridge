* Queues for reliability tests
DEFINE QLOCAL('RELIABILITY.TEST')
SET AUTHREC PROFILE('RELIABILITY.TEST') OBJTYPE(QUEUE) PRINCIPAL('app') AUTHADD(ALLMQI)

* Set persistente for testing after a shutdown
DEFINE QLOCAL('RELIABILITY.PERSIST') MAXDEPTH(10000) DEFPSIST(YES)
SET AUTHREC PROFILE('RELIABILITY.PERSIST') OBJTYPE(QUEUE) PRINCIPAL('app') AUTHADD(ALLMQI)
